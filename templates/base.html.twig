<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}BookNow{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
    {% endblock %}
</head>
<body>
<!-- Header -->
<div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center mb-2 mb-md-0 text-dark text-decoration-none">
            <h1 class="text-primary">BookNow</h1>
        </a>

        <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
            <li><a href="/" class="nav-link px-2 link-secondary">Accueil</a></li>
            <li><a href="{{ path('app_booking_cancelation') }}" class="nav-link px-2 link-secondary">Une annulation ?</a></li>
            <div class="btn-group">
                <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    Services proposés
                </button>
                <ul class="dropdown-menu">
                    {% for service in services %}
                        <li><a class="dropdown-item" href="{{ path('service_show', { id: service.id }) }}">{{ service.name }}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </ul>
    </header>
</div>

<!-- Main Content -->
<main class="d-flex flex-column min-vh-100">
    {% block content %}

    {% endblock %}
</main>

<!-- Footer -->
<footer class="py-5 mt-4" style="background-color: #212529; color: white;">
    <p class="text-center">&copy; 2024 Company | Développé par Pépin Maëlic</p>
</footer>

<!-- Bootstrap JS bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
